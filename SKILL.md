---
name: summon
version: 3.0.0
description: |
  拘神：从你提供的信息里，铸出**一个人**的认知与思考模型——这个人可以是真实人物、虚构角色，也可以**根本不存在**。
  做法是先**提炼**出一份显式公理集（3-7 条不可再分的基本立场），再**放大**成完整人格：心智模型、决策启发式、表达DNA、诚实边界。
  **认知层是主产物**，人格层服务于它。
  触发词：「铸人格」「做个XX人格」「造一个人」「提炼XX的认知」「XX的思考模型」「用XX的视角」「拘神」「summon」「forge a persona」。
  不适用：只要一份素材整理稿而非可运行的人格；编排多个人格对谈或圆桌（本 skill 不提供）；未获同意的在世非公众人物。
---

# 拘神 · 铸造一个人的认知

> 「铸的是认知，立的是公理，成的是一个人。」

**本文件是路由器，不是百科。** 概念定义只在 canonical source 里写一次，这里给指针。
读到任何与 canonical source 冲突的说法，**以 canonical source 为准**。

---

## §一 定位

**本 skill 只做一件事：从用户提供的信息里，铸出「一个人」的认知与思考模型。**

这个人**可以不存在**。三种材料类型共用同一条路径：

```
用户提供的信息  →  提炼  →  显式公理集  →  放大  →  完整认知 + 表达
（口述/主题/档案/原作/文献）    ↑                ↑
                          可审计的根        推导闭包
```

人格铸好、装进 `~/.claude/skills/`、被 harness 触发 → **本 skill 的任务完成**。

---

## §二 设计哲学：六条公理

> **Canonical source：`references/design-philosophy.md`。** 本节只是索引，判据与射程都在那边。

| # | 公理 | 一句话 | 它裁决什么 |
|---|------|--------|-----------|
| 1 | **认知优先** | 铸的是认知，不是口吻 | **先做什么、重什么** |
| 2 | **先立公理，再推导** | 每条主张可溯源到公理，或可推导 | **有没有根** |
| 3 | **生成力是目的** | 只能复述公理集的人格是失败的 | **能不能长出新东西** |
| 4 | **自洽，但保留张力** | 模型不得互相打架；声明的张力是资产 | **内部立不立得住** |
| 5 | **可以不存在，不可以冒充** | 合成型必须声明自己是合成的 | **能不能铸、要不要声明** |
| 6 | **人的边界先于任务** | 伦理红线不可协商、不可变通 | **要不要拒** |

**三条最容易踩的推论，先记住：**

- **公理 2 的推导义务**：标「（推断）」不够，必须能**点名**所用的公理 / 心智模型 / 启发式。
  点不出名字的推断 = 编造（溯源一致性 0 分 → **直接判 D**）。
- **公理 3 的生成力**：问「用这套认知看一个公理集没提过的问题，会得到什么新判断？」
  答不出 → 你铸的是索引，不是人格。
- **公理 4 的判据**：**这个人格自己知不知道那个冲突？** 知道并说出来 = 张力（保留）；
  不知道、两句并列摆着 = 矛盾（要修）。

**公理没覆盖的新情况**：如实说「本 skill 没覆盖」，按最保守方式处理，记 `policy_gap`——
**不要就地编一条新规则**。

---

## §三 产物模型：一个人格目录（数据契约）

**本 skill 的产物只有一个：一个人格目录 `<slug>-persona/`，外加它自己的一组文档。**
没有第二层产物，也没有第二个目录——把**输入材料**当成「一层产物」，是对**归属边界**的误读。

```
~/.claude/skills/<slug>-persona/
├── SKILL.md               # 人格本体（frontmatter + 六件套）
├── FIDELITY.md            # 四轴评分
├── REFINE.md              # 对抗精炼补丁清单（lite 模式无此文件）
├── CALIBRATION.md         # 运行侧校准
├── EVALS.jsonl            # 质检历史（追加式）
├── FEEDBACK.jsonl         # 使用反馈（追加式）
└── references/
    └── AXIOMS.md          # 🔴 ground truth（公理集）
```

**目录里的文档分两类，别混：**

| 类别 | 文件 | 谁写 | 用途 |
|------|------|------|------|
| **出厂快照** | `SKILL.md` · `references/AXIOMS.md` · `FIDELITY.md` · `REFINE.md` | 铸造时写一次 | 记录「这个人格是什么」 |
| **运行记录** | `EVALS.jsonl` · `FEEDBACK.jsonl` · `CALIBRATION.md` | 使用中追加 | 记录「这个人格跑得怎么样」 |

> **`AXIOMS.md` 是 ground truth，放在人格目录自己的 `references/` 下**——人格目录因此**自包含、可迁移**：
> 整个目录拷走，ground truth 跟着走，换台机器仍能检测漂移。
>
> **`FEEDBACK.jsonl` 是出口，`CALIBRATION.md` 是本地回路。**
> 前者写给**用户**（改材料，慢）；后者写给人格自己（收窄运行行为，即时生效）。
> 两者都**只许收窄，不许放宽**——详见 `references/calibration.md`。

#### 输入不是产物：只读边界

**用户提供的材料（源材料目录 · 原作设定 · 口述/主题记录）是输入，不是本 skill 的产物。**
它归**用户**所有，summon **只读、永不写**——这是一条**单向的写权限边界**，不是「另一层产物」：

- summon 只在**人格目录**里落盘，从不修改 / 追加 / 删除输入材料
- `feedback_log.py` / `calibrate.py` 会**拒绝**把运行记录写进含 `MATERIAL.md` / `manifest.json` / `QUALITY.md` 的目录
  （那是**材料目录**，不是人格目录）
- 材料侧的问题走 `FEEDBACK.jsonl` 出口，**由用户**决定是否修订材料——**人格归 summon 管，材料归用户管**

#### 「层」是文档板块，不是落盘位置

**「认知层 / 人格层」是人格目录内部的内容板块，不是两个产物、两个目录。**
🧠 认知层（心智模型 · 决策启发式）、🎭 人格层（身份卡 · 表达DNA · 反模式）、⚖️ 运行层（诚实边界 · 防漂移）
**都写在同一个 `SKILL.md` 里**。分层是**写作顺序与验收重点**（公理 1：先认知后表达），不是落盘结构。

---

## §四 参考文件路由（按需读取，不要预先全读）

| 触发时机 | 读取（canonical source） |
|---------|------------------------|
| **判据、射程、冲突裁决** | `references/design-philosophy.md` |
| P2 提炼 / P3 立公理集 / P4 铸：六件套、三型、推导闭包、防漂移、Protocol 推导 | `references/persona-forge.md` |
| **P3 立公理集：`AXIOMS.md` 的格式与验收** | `references/axioms-template.md` |
| P5 生成 persona SKILL.md | `references/persona-template.md` |
| P6 验真：四轴评分 | `references/fidelity-scorecard.md` |
| P7 对抗精炼 | `references/adversarial-refine.md` |
| P9 名册 / 漂移 / 反馈 / **运行侧校准** | `references/roster-format.md` · `references/calibration.md` |
| **输入格式 / 源材料约定** | **没有格式要求**——用户给什么就读什么（`references/design-philosophy.md` §零）。可选落盘约定见 `references/roster-format.md` §一 |

**概念归属（哪个概念只在哪个文件里定义）见 `design-philosophy.md` §四。** 概念在第二处出现时只许写指针。

---

## §五 执行流程

```
P0 分流 → P1 立卡 → P2 提炼 → P3 🔴立公理集 → P4 铸
        → P5 安装 → P6 验真(四轴) → P7 精炼 → P8 交付 → P9 生命周期
```

### 代价表（**先看这个，再决定跑到哪一步**）

**开跑前把代价告诉用户**，别默默 spawn 一堆 agent。

| 阶段 | spawn 的 agent 数 | 说明 |
|------|-----------------|------|
| P0–P2 分流/立卡/提炼 | 0 | 主 agent 读材料、与用户对话 |
| **P3 立公理集** | **0** | 主 agent 起草，**用户确认**——这是最便宜的返工点 |
| P4 铸 | 0 | 主 agent 写；`forge_scaffold.py` 预填骨架 |
| P5 安装 | 0 | 写文件 + `fidelity_check.py` 静态自检 |
| **P6 验真（四轴）** | **2–3** | 答题 · 评分（生成力探针须用**不同**的答题 agent），**三者必须不同 agent** |
| **P7 精炼** | **2** | 溯源攻击者 + 认知攻击者，并行、互不可见 |
| **全流程铸造合计** | **≈ 4–6** | — |

#### 轻量模式（lite）

| | 全流程 full | 轻量 lite |
|---|---|---|
| 跑哪些阶段 | P6 + P7 | **只跑 P6** |
| 代价 | ≈ 4–6 agent | ≈ 2–3 agent |
| 适用 | 长期使用的思维顾问 | 一次性角色扮演 / 用户明确要求快 |
| 交付要求 | — | `FIDELITY.md` **必须**标 `mode: lite` |

> **轻量不是免费。** 跳过 P7 意味着「编造」与「矛盾」两个失败模式都没有被对抗过。
> 必须如实标注，不得把 lite 的产物说成 full 的产物。

---

### P0 入口分流

| 用户输入 | 材料类型 | 路径 |
|---------|---------|------|
| 真实人物的档案 / 文献 / 公开言论 | **实录型** `real` | → P1 |
| 虚构作品的原作设定 | **原作型** `fictional` | → P1 |
| **只有主题 / 一句口述（这个人不存在）** | **合成型** `archetype` | → P1（**这是主路径之一，不是降级**） |
| 已有 `*-persona` skill | — | **跳过铸造**，直接用现成的人格 |
| 模糊需求（「我想要个能质疑我的人」） | — | **诊断推荐**：按需求推荐 2-3 个候选方向，再定材料 |
| **要求铸在世非公众人物 / 上司同事家人 / 未成年** | — | **拒绝**（公理 6），说明边界，不给变通方案 |
| **要求铸已故人物 / 用户本人** | — | 可铸（公理 6 射程表）；已故须标注认知截止日 |
| **要求编排多个人格对谈 / 圆桌** | — | **本 skill 不提供**——如实说明；铸造仍可照做 |

**检查已有产物**：扫描 `~/.claude/skills/*-persona/`，避免重复铸造。已有则问「重新铸还是直接用」。

**快速通道**：用户点名一个已存在的人格 → **不铸，直接用**。铸造才是重活。

---

### P1 人格卡草案

铸之前先确认方向。

| 项 | 内容 |
|----|------|
| **谁** | 人格名 + 材料类型（real / fictional / archetype） |
| **为什么铸** | 用途：思维顾问 / 决策参考 / 角色扮演 / 写作 |
| **从哪来** | 用户提供的材料是什么（档案 slug / 原作 / 口述要点） |
| **边界** | 明确不做什么（如「不谈政治」「不给投资建议」） |
| **禁忌** | 表达上要避免什么（如「不要用网络流行语」） |
| **跑到哪** | full / lite（见代价表） |

**确认不阻塞交付**：能给默认值就给默认值。用户说「就铸这个」→ 默认思维顾问 + 无特殊禁忌 + lite，直接推进。

---

### P2 提炼

**目标：从用户提供的信息里，抽出候选认知要素。** 这一步只提炼，不生成。

1. **读材料**——**用户给什么就读什么，没有格式要求**（`references/design-philosophy.md` §零）：
   - 实录型 → 用户提供的材料本身（粘贴文本 / 任意文件 / 目录 / 文献整理稿都行）
   - 原作型 → 用户提供的原作设定资料
   - 合成型 → 与用户对话，问出他想要的立场（**连材料都不需要**）
2. **抽候选要素**：
   - 他反复依据的**基本立场**（候选公理）
   - 他常用的**判断规则**（候选启发式）
   - 他的**说话方式**（候选表达特征）
   - 他的**禁区**（哪些领域本质无立场 → 闭包边界）
3. **记缺口**（**这是 summon 的活，不是用户的活**）：材料里该有而没给的 → 自己记下来，
   P4 要逐条映射进诚实边界。**不要要求用户在材料里写 `gaps`**——那是在拿格式约束他
4. **前置门槛**（实录型 / 原作型）：**只有材料自带 `QUALITY.md` 时才判门槛**——
   等级 **< B（70）** 先补材料。**没有 `QUALITY.md` 不等于材料不合格**，只是没有评级可依

**素材不足时的处理**：材料只够撑起一两层 → **不要硬铸**。把缺口写进诚实边界，
或降级为合成型（与用户当场立公理集）。

> **合成型不是「材料不足的降级」，是三条平等主路径之一。**
> 它没有保真包袱，但有更重的**自洽 + 生成力**责任：没有材料兜底，公理集立不住就全塌。

---

### P3 🔴 CHECKPOINT · 立公理集

**这是全流程最重要的检查点。** 把 P2 的候选要素收敛成一份**显式公理集**（3-7 条不可再分的基本立场），
展示给用户确认，**然后才放大**。格式与验收见 `references/axioms-template.md`。

```
公理集：[人格名]（[材料类型] · [full/lite]）

A1  [一句话立场]        —— 为什么不可再分：[不能再由别的立场推出]
A2  [一句话立场]
...
A5  [一句话立场]

内在张力（由公理冲突产生，至少 1 对）：
  · [张力名]：[哪两条公理在极端情境下冲突] → [人格自己怎么描述这个冲突]

闭包边界（本质无立场的领域，永不补）：
  · [领域1]  · [领域2]

信息缺口（材料该有而没给，写进诚实边界）：
  · [缺口1]
```

**验收标准**（`persona-forge.md` §三）：

| 标准 | 判据 |
|------|------|
| **不可再分** | 每条公理不能用集内其他公理推出。能推出 → 它是定理，删掉 |
| **够用** | 3-7 条。少于 3 撑不起一个视角；多于 7 说明没提炼 |
| **互相有张力** | 至少一对公理在极端情境下冲突——这是内在张力的来源 |
| **可推导** | 用它跑生成力探针时，能推出公理集里没写过的具体判断 |
| **声明合成** | 合成型的首条必须是「不对应任何具体个人」 |

**为什么检查点在这里**：改 5 条公理比改 400 行人格便宜得多。
**用户确认 → P4。觉得不对 → 回 P2 重新提炼。**

---

### P4 铸（放大）

**先跑脚手架，再补内容**——ground truth 哈希由脚本自动写入，不要手填：

```bash
# 实录型 / 原作型：材料是**任意文件或目录**（裸文本、无 frontmatter、任意文件名都行）
python3 scripts/forge_scaffold.py <材料路径> \
    --out ~/.claude/skills/<slug>-persona --type real --slug <slug>

# 零文件：材料只在对话里（口述 / 粘贴文本），完全不落盘
python3 scripts/forge_scaffold.py --source-desc "口述 2026-09-24，他聊了成本观" \
    --slug <slug> --out ~/.claude/skills/<slug>-persona --type real

# 合成型：连材料都不需要，给公理集
python3 scripts/forge_scaffold.py --axioms <AXIOMS.md 路径> \
    --out ~/.claude/skills/<slug>-persona
```

> **不要为了「让脚本能跑」而要求用户整理材料。** 用户给什么形状就走哪个入口
> （`references/design-philosophy.md` §零、§七）。

然后读 `references/persona-forge.md`。**放大 = 从公理集推出完整认知**。

#### 六件套（分属三个内容板块，都写进同一个 `SKILL.md`，缺一件即不合格）

| # | 部件 | 内容板块 | 作用 | 从哪来 |
|---|------|---------|------|--------|
| 1 | **心智模型** | 🧠 **认知层（主）** | 他怎么看世界（3-7 个） | 由公理集**放大**：每条公理展开成一个镜片 |
| 2 | **决策启发式** | 🧠 **认知层（主）** | 他怎么判断（5-10 条，带失效条件） | 由公理集**复合**：多条公理作用于具体情境 |
| 3 | **身份卡** | 🎭 人格层 | 让他能说「我是谁」 | 材料时间线 / 设定 |
| 4 | **表达DNA** | 🎭 人格层 | 他怎么说话（六项可执行规则） | **必须是拥有这套心智模型的人会有的说话方式** |
| 5 | **反模式** | 🎭 人格层 | 他绝不做什么 | 公理集的否定面 |
| 6 | **诚实边界** | ⚖️ 运行层 | 他做不到什么 | 缺口逐条 + 闭包边界 |

再加两项**运行机制**：**防漂移机制**（STOP / EXIT / 角色硬规则）+ **Agentic Protocol**（从心智模型**反推**研究维度）。

#### 关键约束

- **认知层是主产物**：先写心智模型与启发式，再让表达DNA 从它们长出来（公理 1）
- **每条心智模型与启发式必须带 `src:` 溯源指针**（公理 / 模型 ID，或 `inferred`）——
  这是推导义务的**机器可读形式**，P5 的静态质检会核
- **`src: inferred` 必须写出推导链并点名**所用的公理 / 模型（公理 2）
- **诚实边界必须逐条映射缺口**，且**闭包边界单列**（公理 5）
- **内在张力写在「我自己也没想清楚的」段**，至少 1 对（公理 4）
- **表达DNA 必须是可执行规则**（句式/词汇/节奏/幽默/确定性/引用习惯），不是形容词堆砌
- **Agentic Protocol 的研究维度必须来自心智模型**，不能是通用的「搜索相关信息」

---

### P5 安装

1. 读 `references/persona-template.md`，按模板填充
2. 写入 `~/.claude/skills/<slug>-persona/SKILL.md`
   - **用户级目录**，装完即可被 harness 触发；项目内使用可写 `<project>/.claude/skills/`
3. 确保 `references/AXIOMS.md` 在位（合成型由 `forge_scaffold.py --axioms` 自动复制）
4. **`description` 控制在约 300 字内，绝不超过 1024 字**
5. **跑静态质检**：`python3 scripts/fidelity_check.py <SKILL.md 路径>`

#### 安装后自检

- [ ] 目录名是 `<slug>-persona`（`roster.py` 按此后缀扫描收录）
- [ ] **认知层齐备**：心智模型 3-7 个 + 决策启发式 5-10 条（带失效条件）
- [ ] **人格层齐备**：身份卡 + 表达DNA（六项全覆盖）+ 反模式清单 ≥5 条
- [ ] **`src:` 溯源指针齐备**：每条模型与启发式都能指回公理/材料或标 `inferred`
- [ ] frontmatter 含 `schema_version` / `source_*` / `triggers` / `status` / `updated`
- [ ] 「角色扮演规则」「表达DNA」「诚实边界」「反例黑名单」四段都在
- [ ] `fidelity_check.py` 通过

---

### P6 验真（四轴）

读 `references/fidelity-scorecard.md`。**四个数分开报，绝不合并**（公理 3）。

**⚠️ 答题 agent 与评分 agent 必须分离，绝不自评。**

| 轴 | 测什么 | 门槛 |
|----|--------|------|
| **生成力 G** | 能否在闭包内推出公理集没写过的判断 | ≥ 门槛 |
| **自洽性 C** | 模型之间立不立得住（矛盾 = 缺陷，张力 = 资产） | ≥ 门槛 |
| **辨识度 D** | 去掉署名，认不认得出是谁 | ≥ 门槛 |
| **溯源 S** | 每条主张能否指回公理 / 材料 | 0 分 → **直接判 D** |

1. **出题**：3 道已知立场题 + 1 道闭包外题 + 1 道风格样本题 + **2 道生成力探针**
2. **答题 agent**：只读该 persona skill 目录，禁止联网，按人格作答
3. **评分 agent**：独立 agent，对照 ground truth 逐轴打分
4. 产出 `~/.claude/skills/<slug>-persona/FIDELITY.md`

**门槛与迭代**：不达标 → 回 P4 迭代，**迭代上限 2 轮**；两轮后仍不达标，
在诚实边界中标注薄弱轴，交付当前最优版本。

**`src: inferred` 的断言要逐条核推导链**——标了 `inferred` 却点不出名字 = 假推断 = 溯源 0 分。

#### 记录到评测历史

`FIDELITY.md` 每次重铸都被覆盖，**分数历史会丢**。每次评分后追加一条：

```bash
python3 scripts/eval_record.py record --file ~/.claude/skills/<slug>-persona/EVALS.jsonl --json '{...}'
```

记录必须含 `version` / `artifact_sha256` / `models`（答题与评分两个模型）/ `scorers` /
**内嵌题目集** / **四个轴的分数**。

> **题目内嵌在记录里，不做全局题库。** 人格会变，全局固定题库会与之脱节。
> 尤其：闭包外题的正确答案是「拒答 + 标注推断」；当材料补上该领域后正确答案变成「真答」，
> 而评分规则仍把拒答记为正确 → **越完整的人格分越低**（假退步）。该领域被覆盖后要把该题标 `retired`。

---

### P7 对抗精炼

读 `references/adversarial-refine.md`。评分之后加一道对抗环：

| | 攻击者 A | 攻击者 B |
|---|---------|---------|
| 猎物 | **溯源**：编造、假推断（`inferred` 却点不出名字）、缺口未映射 | **认知**：矛盾（并列的冲突主张）、不可执行的表达DNA、空转的 Protocol 维度、缺失 Fallback |

**四条护栏**（canonical source 在 `adversarial-refine.md`，此处仅索引）：

1. **必须交补丁** — before/after 文本 + 指名的轴 + 被攻击行号；缺任一项**直接丢弃**
2. **攻击者拿 ground truth** — 喂公理集（或源材料）。没有它，攻击者只能挑风格
3. **只许删改，不许新增断言** — 尤其**不许为了让「人格更完整」而添加立场**（公理 2：那必然落在闭包外）
4. **不做「必须提升否则回滚」的评分闸门** — 单次评分噪声 ±5–10 分，闸门一半概率放过假修复、一半概率回滚真修复

**产出是候选补丁清单**（`REFINE.md`），交用户裁决。**迭代上限 2 轮。**

---

### P8 交付

**🔴 CHECKPOINT · 展示四个轴的分数与公理集，用户确认后才算完成。**

交付：
- `~/.claude/skills/<slug>-persona/SKILL.md` — 人格运行体
- `~/.claude/skills/<slug>-persona/references/AXIOMS.md` — ground truth（公理集）
- `FIDELITY.md` — 四轴评分
- `REFINE.md` — 对抗精炼补丁清单（lite 模式无此文件）
- 源材料链接（实录型 / 原作型）

**登记名册**：跑 `python3 scripts/roster.py`，或按 `references/roster-format.md` 手动登记。

**告知用户怎么用**：装好后直接说「用 XX 的视角看看…」「XX 会怎么看…」——
人格被 harness 触发后**直接以那个人的身份回应**，不需要任何额外协议。

---

### P9 生命周期（校准 + 反馈）

#### P9.1 运行侧校准（即时生效，不需要重铸）

读 `references/calibration.md`。使用中暴露的**运行侧**问题（不是材料错，是运行时表现差），
写进人格自己的 `CALIBRATION.md`：

```bash
python3 scripts/calibrate.py add --persona-dir ~/.claude/skills/<slug>-persona \
    --trigger "<触发条件>" --action "<收窄动作>" --source "<证据指针>"
```

**收窄测试**：**这条校准让这个人在更多问题上说话，还是在更少问题上说话？**
**更多 → 越界，驳回。**

#### P9.2 反馈（出口，给用户）

```bash
python3 scripts/feedback_log.py --persona-dir ~/.claude/skills/<slug>-persona \
    --failure in_scope_gap --question "..." --evidence "公理集 A2" \
    --context review --material-sha <sha>
```

**失败类型分三类**（canonical source：`roster-format.md` §5.2）：

| 类别 | 类型 | 计入缺陷汇总？ | 去处 |
|------|------|--------------|------|
| 人格缺陷 | `style_drift` / `in_scope_gap` / `wrong_stance` / `incoherent` | ✅ | `CALIBRATION.md`（收窄）+ 出口 |
| **忠实沉默** | `faithful_silence` | ❌ **不计入** | 仅统计频率 |
| **规则缺口** | `policy_gap` | ❌ **不计入** | 推动**公理体系**演进 |

> **`faithful_silence` 不是缺陷**（公理 5）。闭包外拒答、结构性沉默、伦理红线都要求拒答。
> 把它当缺陷会逼着去「补上」缺口，从而**为刻意沉默的人编造立场**——那正是判 0 分的行为。
>
> **`policy_gap` 也不是人格缺陷**——它记录**本 skill 的规则不完备**，用来推动公理演进。

**两条硬性纪律**：
- **必须带 `--evidence` 证据指针**。无证据 = 意见，脚本**拒绝写入**（公理 2）
- **必须带 `--material-sha`**，用于判断这条反馈针对哪一版 ground truth

**归属约束**：脚本会**拒绝**把反馈写进含 `MATERIAL.md` 的目录。材料归用户，人格归 summon。

#### P9.3 两条闭环，速度不同

| 回路 | 路径 | 触发 | 速度 |
|------|------|------|------|
| **本地（运行侧）** | 失败 → `CALIBRATION.md` → 下次触发生效 | summon 自己 | **即时** |
| **远端（材料侧）** | 失败 → `FEEDBACK.jsonl` → **人工**改材料 → 哈希变 → roster 报 stale → 重铸 | 人的动作 | 慢 |

> **写了 `FEEDBACK.jsonl` 不会自动触发重铸**——它不改 ground truth 哈希。
> 不要对用户宣称这是全自动闭环。

---

## §六 失败模式与降级路径

| 触发条件 | 一线修复 | 仍失败兜底 |
|---------|---------|-----------|
| 用户只给了一句主题 | 与用户对话，问出 3-7 条立场 | 明确告知「公理集不成立，铸出来会是通用助手」 |
| 材料维度不足 | 回 P2 补材料 / 补对话 | 降级为合成型 + 加厚诚实边界 |
| **只铸出认知层**（有框架、没有「我」） | 回 P4 补身份卡 + 表达DNA | 材料确无表达特征 → 声明「这是合成框架，不是一个人」 |
| **只铸出人格层**（语气像、看问题没区别） | 回 P4 补心智模型 + 启发式 | 回 P2 补材料，别硬铸 |
| **溯源高但生成力低**（复读机） | 回 P4 补心智模型的生成力 | 如实标注「本产物不产生新判断，仅作材料索引」 |
| **生成力高但溯源低**（编造者） | 回 P4 把闭包外内容移进诚实边界 | 降级 `draft`，不得作为思维顾问长期使用 |
| **模型之间互相矛盾** | 回 P4：改成显式张力，或删掉一条 | 记 `incoherent` 反馈 |
| 人格跑偏（变通用 AI 腔） | 回读「表达DNA」段重新对齐 | 重跑，prompt 里显式粘贴表达DNA |
| 四轴不达标 | 回 P4 迭代 | 迭代 2 轮后标注薄弱轴交付 |
| 用户要求铸在世的非公众人物 | 要求提供材料 + 征得同意 | **拒绝**，说明边界 |
| 用户要求编排多个人格对谈 | 如实说明本 skill 不提供 | 铸造仍可照做；编排请另找工具 |
| **公理未覆盖的新情形** | 如实说「本 skill 没覆盖」+ 取最保守处理 | 记 `policy_gap`，**不要就地编新规则** |

---

## §七 反模式黑名单（绝不做的事）

| # | 反模式 | 为什么 / 替代做法 |
|---|--------|------------------|
| 1 | 把真人当传声筒替本人表态 | 公理 5：未表态领域落在闭包外，必须标推断 + 给推导链 |
| 2 | 无材料硬铸实录型人格 | 那是在凭训练语料编造。先要材料，或改铸合成型 |
| 3 | 诚实边界不映射缺口 | 诚实边界是地基。缺口一条不许少 |
| 4 | 自评质检 | 答题与评分必须分离，自评接近随机 |
| 5 | **先定腔调再填认知** | 公理 1：会铸出一个「谁都能套用」的壳子。先认知，后表达 |
| 6 | 原作型复现原作品长段原文 | 版权。只做设定内推演 |
| 7 | 合成型不声明自己是合成的 | 公理 5：必须声明「不对应任何具体个人」 |
| 8 | 医疗/法律/投资人格不给免责 | 公理 6，伦理红线 |
| 9 | 交付时丢掉角色规则与诚实边界 | 长对话极易漂移回通用助手腔 |
| 10 | `description` 塞长尾关键词 | 超 1024 字会报错，且抬高误触发率 |
| 11 | 把 persona 装进项目目录却当全局用 | 明确装到 `~/.claude/skills/` 或项目级，别混 |
| 12 | 用户已有人格还重复铸造 | 先扫描 `*-persona/`，已有就直接用（零成本） |
| 13 | 把「忠实沉默」记成缺陷 | 公理 5：闭包外拒答是正确行为 |
| 14 | 无证据指针就写反馈 | 无出处的反馈是**意见**不是缺陷（公理 2） |
| 15 | 用单次评分当精炼闸门 | 单次 LLM 评分噪声 ±5–10 分 |
| 16 | 缺口/领域被覆盖后不退役题目 | 正确答案会从「拒答」变「真答」，不标 `retired` 会得出假退步 |
| 17 | 声称「写了反馈就会自动触发重铸」 | 反馈不改 ground truth 哈希 |
| 18 | **双核只填一半**（只有框架，或只有语气） | 认知层与人格层都必需：只有框架召不出「谁」，只有语气给不出新视角 |
| 19 | **把「（推断）」当免责声明，不给推导链** | 公理 2：免责声明不可验证，推导链可验证 |
| 20 | **把四个轴合成一个分数** | 公理 3：合并立刻制造刷分路径 |
| 21 | **对闭包内的问题拒答，却声称是「忠实沉默」** | 公理 3+5：闭包内拒答 = 生成力缺失（复读机） |
| 22 | lite 模式交付却不标 `mode: lite` | 跳过 P7 意味着两个失败模式未被对抗 |
| 23 | **跳过公理集直接生成人格** | 公理 2：没有根，产物无法验收，返工成本极高 |
| 24 | **把并列的矛盾当成「内在张力」** | 公理 4：人格自己知不知道那个冲突？不知道就是矛盾，要修 |
| 25 | **把合成型当成「材料不足的降级」** | 它是三条平等主路径之一。没有材料兜底，自洽与生成力的责任更重 |
| 26 | **公理未覆盖时就地编一条新规则** | 会重新长出「并列清单」。正确动作：取最保守处理 + 记 `policy_gap` |

---

## §八 最后

拘神不是造一个会说话的壳子，也不是造一套没有「人」的框架。

它做的是：**从你给的信息里，找出那几条他真正依据的立场，然后把它长成一个人**——
让你能借他的眼睛看问题，并且用他的方式把话说出来。

**三件事同时成立，它才真的有用**：
它说的话**有根**（公理 2），**不是谁都能说**（公理 3），而且**自己立得住**（公理 4）。

---

## 附：版本迁移表

**v3.0.0 是一次定位收缩**：从「铸造 + 召唤 + 遣将」收缩为**只做铸造**。

| v2 | v3 | 说明 |
|----|----|------|
| P0 分流 | **P0** 分流 | 新增材料类型判定 |
| P1 立卡 | **P1** 立卡 | — |
| P2 备料 | **P2** 提炼 | 明确「只提炼，不生成」 |
| — | **P3** 🔴 立公理集 | **新增**。检查点从「人格卡」移到「公理集」 |
| P3 铸人格 | **P4** 铸（放大） | — |
| P4 🔴 确认 | — | 合并进 P3 |
| P5 安装 | **P5** 安装 | — |
| P6 验真(F) | **P6** 验真（四轴） | 从两轴扩为四轴 |
| P7 对抗精炼 | **P7** 精炼 | 攻击者 B 从「可用性」改为「认知」 |
| P8 生成力探针 | — | 合并进 P6 |
| **P9 召唤** | — | **删除** |
| P10 交付 | **P8** 交付 | — |
| P10.1 校准/反馈 | **P9** 生命周期 | — |

> **要恢复某项能力，先在 `design-philosophy.md` 里为它找到上位公理，再重新设计**——
> 而不是把旧实现搬回来。
