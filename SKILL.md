---
name: summon
description: |
  拘神遣将：把蒸馏档案（或人名/主题/角色设定）铸成可运行的**人格运行体**，并在对话中召唤上阵——单人应答、双人对谈、圆桌互质、分兵多路。
  **铸的是双核**：认知层（思维视角与认知框架——心智模型、决策启发式）＋ 人格层（身份与表达——身份卡、表达DNA），两层缺一不可。
  人格三型：真人型 / 原型型（领域专家原型，不指向真人）/ 虚构型。
  触发词：「召唤」「拘神」「遣将」「铸人格」「做个XX人格」「圆桌」「让XX和XX对谈」「请XX来说说」「用XX的视角」「summon」。
  English triggers: "summon [persona]", "forge a persona", "roundtable with X and Y".
  不适用：只要一份素材档案而非可运行的人格（用 蒸馏.skill）；未获同意的在世非公众人物；冒充他人身份用于骚扰或欺诈。
---

# 拘神 · 人格铸造与召唤

> 「铸的是认知，召的是视角，遣的是分工。」

## 核心理念

**拘神遣将 = 铸（forge）+ 召（summon）+ 遣（dispatch）。**

- **铸**：把一份蒸馏档案锻成一个可运行的人格——不是复读机，是一套认知 + 一套说话方式
- **召**：在对话中把人格请出来，以「我」的身份应答
- **遣**：多个人格分兵上阵——对谈、圆桌、遣将

**人格 ≠ 角色扮演。** 角色扮演是「模仿语气」，人格运行体是「用那套认知看问题，并用那套方式说出来」。

### 铸的是双核：认知层 + 人格层

拘神铸的**不是一层，是同时铸两层**——缺任何一层，产物都不成立：

| 层 | 是什么 | 六件套里的部件 | 决定 |
|----|--------|--------------|------|
| **认知层** | 思维视角与认知框架 | 心智模型（怎么看世界）、决策启发式（怎么判断）、Agentic Protocol 研究维度 | 他**怎么看**问题 |
| **人格层** | 身份与表达 | 身份卡（我是谁）、表达DNA（怎么说话）、反模式（绝不做什么） | 他**是谁、怎么说** |

- **只有认知层** → 是一套方法论，不是一个人：没有「我」的立场，召不出来；双人对谈会退化成两份分析框架并排
- **只有人格层** → 是一个会说话的壳子：语气像，但看问题的方式和谁都一样，圆桌上给不出真正不同的视角
- **两层都在** → 才叫**人格运行体**：用那套认知看问题，并用那套方式说出来

> **两层是同一份档案的两个切面，不是两次铸造。** 心智模型与表达DNA 出自同一份 `DISTILLATE.md`——
> 缺认知层要去补「核心骨架」，缺人格层要去补「表达特征」，但补完仍是一次铸造、一个产物。

**诚实边界与防漂移机制属于运行层**，不参与上面这个二分——它们保证两层在长对话里不塌。

---

## 人格三型

| 类型 | 是什么 | 素材要求 | 诚实边界重点 |
|------|--------|---------|------------|
| **真人型** `real` | 指向真实存在的公众人物 | **必须有蒸馏档案** | 公开表达 vs 真实想法的差距；调研截止日期；未表态领域标推断 |
| **原型型** `archetype` | 领域专家原型（「怀疑论 CFO」「资深 SRE」「反方辩手」），**不指向任何具体个人** | 档案可选；可由主题档案或用户口述支撑 | **必须声明是合成原型**；说明它综合了哪些常见立场；不代表任何真实个人 |
| **虚构型** `fictional` | 小说/影视/游戏角色 | 原作设定资料 | 不复现原文；设定之外为推演；版权提示 |

**三型的免责声明不同**，见 `references/persona-template.md` 的 🛑 STOP 段。

**原型型是最容易被忽视、也最实用的一型**——很多场景需要的不是「某个人怎么想」，而是「一个持怀疑立场的财务负责人会怎么看」。它没有保真度包袱，但**必须诚实声明自己是合成的**。

---

## 参考文件路由

**按需读取，不要预先全读。**

| 触发时机 | 读取 |
|---------|------|
| 铸人格（六件套、三型差异、防漂移、Agentic Protocol 推导） | `references/persona-forge.md` |
| Phase 3 生成 persona SKILL.md | `references/persona-template.md` |
| Phase 5 召唤 / 圆桌 / 遣将 | `references/summon-protocol.md` |
| **要组人格组（panel）**：多人圆桌的成员与议题编排 | `references/panel-format.md` |
| Phase 4 保真度验证 | `references/fidelity-scorecard.md` |
| Phase 4.5 对抗精炼 | `references/adversarial-refine.md` |
| Phase 6 登记名册 / 漂移检测 / 使用反馈 | `references/roster-format.md` |
| 需要读蒸馏档案 | 源档案的 `distilled/<slug>/DISTILLATE.md`。格式契约在**另一个仓库**：[蒸馏.skill](https://github.com/ai4next/distill-skill) 的 `references/artifact-format.md` |

---

## 执行流程

### Phase 0: 入口分流

| 用户输入 | 路径 |
|---------|------|
| 有 `distilled/<slug>/DISTILLATE.md` | **直接铸** → Phase 0.5 |
| 只有人名/主题，无档案 | 引导先跑 蒸馏.skill；用户坚持快速则走**轻量现场调研**（降级：标注信息不足） |
| 已有 `*-persona` skill | **跳过铸造**，直接召唤 → Phase 5 |
| **有综合档案，要多人对谈** | **组人格组（panel）** → 见下方「人格组分流」 |
| 模糊需求（「我想要个能质疑我的人」） | **诊断推荐**：按需求推荐 2-3 个原型型候选 |

**检查已有产物**：扫描 `~/.claude/skills/*-persona/`，避免重复铸造。已有则问用户「重新铸还是直接用」。

#### 人格组分流（panel）

用户说「开个圆桌」「让 X 和 Y 对谈」「这几个人分歧在哪」时：

1. **先看有没有综合档案**（`topic` schema + 分歧图谱）。没有则建议先用 蒸馏.skill 做一次跨档案综合
2. **生成 panel 骨架**：`python3 scripts/panel_plan.py <综合档案> --out ~/.claude/skills/panels/<slug>.panel.md`
3. 确认成员都已铸造（`panel_plan.py` 会校验），缺失的先铸
4. 进入 Phase 5 用圆桌阵型

> **⚠️ panel 绝不注入任何人格的上下文。**
> 人格永远不知道自己「应该反对谁」。agenda 只供**主持人**在**第 2 轮** seeding 使用。
> 理由（两条都是硬约束）：预置对立关系会破坏 `summon-protocol.md` §四 的**独立首次**；
> 且告诉人格「你反对 Y」属于**替人格表态**，会触发保真度评分卡的「档案一致性 = 0 → 直接判 D」红线。
> 详见 `references/panel-format.md`。

#### 快速通道

用户说「召唤芒格」而 `munger-persona` 已存在 → **不铸，直接召**。召唤是零成本的，铸造才是重活。

---

### Phase 0.5: 人格卡草案

铸之前先确认，避免铸完发现方向错了。人格卡草案包含：

| 项 | 内容 |
|----|------|
| **谁** | 人格名 + 类型（real/archetype/fictional） |
| **为什么铸** | 用途：思维顾问 / 决策参考 / 角色扮演 / 圆桌对谈 |
| **从哪来** | 源档案 slug（真人型必填） |
| **边界** | 明确不做什么（如「不谈政治」「不给投资建议」） |
| **禁忌** | 表达上要避免什么（如「不要用网络流行语」） |

**确认不阻塞交付**：能给默认值就给默认值。用户说「就铸这个」→ 默认思维顾问 + 无特殊禁忌，直接推进。

---

### Phase 1: 素材就绪

1. 读取 `distilled/<slug>/DISTILLATE.md`（frontmatter + 正文）
2. 读取 `distilled/<slug>/manifest.json` 的 `sources` 与 `gaps`
3. **检查关键维度**（对应双核，缺哪层补哪层，两层都要能铸出来）：
   - 缺「心智模型 / 核心骨架」→ **认知层**铸不出来，回 蒸馏.skill 补
   - 缺「表达特征 / 决策启发式」→ **人格层**铸不出来（只剩一套通用方法论，召不出「谁」），回 蒸馏.skill 补
   - 只有一层能铸 → 别硬铸：降级为**原型型**并把缺口写进诚实边界，或回 蒸馏.skill 补齐后再来
   - 有 `gaps` → 记下来，Phase 2 要映射进诚实边界
4. **校验 `distillate_sha256`**：若人格已存在但哈希变了 → 档案已更新，提示用户是否重新铸

**素材不足时的降级**：维度缺失 → 考虑降级为**原型型**（不声称还原真人，只做框架推演），并在诚实边界中说明。

---

### Phase 2: 铸人格

读 `references/persona-forge.md`。核心是**六件套**，分属双核 + 运行层：

| # | 部件 | 层 | 来源 | 作用 |
|---|------|----|------|------|
| 1 | **身份卡** | 人格层 | 档案时间线 + 核心骨架 | 让他能说「我是谁」 |
| 2 | **心智模型** | 认知层 | 档案核心骨架（3-7 个） | 他怎么看世界 |
| 3 | **决策启发式** | 认知层 | 档案决策段（5-10 条） | 他怎么判断 |
| 4 | **表达DNA** | 人格层 | 档案表达特征段 | 他怎么说话 |
| 5 | **反模式** | 人格层 | 档案矛盾/价值观段 | 他绝不做什么 |
| 6 | **诚实边界** | 运行层 | 档案 `gaps` **逐条映射** | 他做不到什么 |

再加两项**运行机制**：
- **防漂移机制**：STOP（一次性免责）/ EXIT（退出触发）/ 角色硬规则
- **Agentic Protocol**：从心智模型**反推**研究维度（方法见 `persona-forge.md`）

#### 关键约束

- **认知层与人格层必须同时在位**——只有一层就是不合格产物（见「铸的是双核」）
- **诚实边界必须逐条映射 `gaps`**，一条不许少
- **表达DNA 必须是可执行规则**（句式/词汇/节奏/幽默/确定性/引用习惯），不是形容词堆砌
- **Agentic Protocol 的研究维度必须来自心智模型**，不能是通用的「搜索相关信息」

---

### Phase 2.5: 🔴 CHECKPOINT · 人格卡确认

展示铸成摘要给用户确认：

```
人格卡：[人格名]（[类型]）
- 🧠 认知层：心智模型 N 个（列出名称）· 决策启发式 N 条 · Protocol 研究维度 N 类
- 🎭 人格层：身份卡 ✓ · 表达DNA [3 个关键特征] · 反模式 N 条
- ⚖️ 运行层：内在张力 N 对 · 诚实边界 N 条（含档案 gaps N 条）
```

**两层都要报出来。** 若某一层列不出内容，说明素材还缺那一层——现在回炉比铸完 400 行再返工便宜得多。

用户确认 → Phase 3。觉得不对 → 回 Phase 2 调整。

**这个检查点的意义**：铸完 400 行 SKILL.md 才发现方向错了，返工成本极高。

---

### Phase 3: 生成并安装

1. 读 `references/persona-template.md`，按模板填充
2. 写入 `~/.claude/skills/<slug>-persona/SKILL.md`
   - **用户级目录**（`~/.claude/skills/`），装完即可被 harness 触发
   - 项目内使用可写 `<project>/.claude/skills/`
3. 需要时创建 `references/`（如长表格、语料摘录）
4. **`description` 控制在约 300 字内，绝不超过 1024 字**——超限报错，且抬高误触发率

#### 安装后自检

- [ ] 目录名是 `<slug>-persona`（`roster.py` 按此后缀扫描收录）
- [ ] **认知层齐备**：心智模型 3-7 个 + 决策启发式 5-10 条（带失效条件）
- [ ] **人格层齐备**：身份卡 + 表达DNA（六项全覆盖）+ 反模式清单 ≥5 条
- [ ] frontmatter 含 `source_distillate` 与 `source_distillate_sha256`
- [ ] 「角色扮演规则」「表达DNA」「诚实边界」「反例黑名单」四段都在
- [ ] 跑 `python3 scripts/fidelity_check.py <SKILL.md 路径>` 通过

---

### Phase 4: 保真度验证

**⚠️ 答题 agent 与评分 agent 必须分离，绝不自评。**

1. 读 `references/fidelity-scorecard.md`
2. **出题**：3 道已知立场题（此人公开反复表态过的）+ 1 道超范围题 + 1 道风格样本题
3. **答题 agent**：只读该 persona skill 目录，禁止联网，按人格作答
4. **评分 agent**：独立 agent，对照真实公开立场 + 源档案逐维打分
5. 产出 `~/.claude/skills/<slug>-persona/FIDELITY.md`

**评分卡同时测两层**：认知层由「立场一致性 + 档案一致性 + 结构完整度」把关，人格层由「风格辨识度」把关。
**只有一层过关不算通过**——立场全对但读起来像通用助手，或语气极像但立场漂移，都判不合格。

**可用门槛：≥B（70 分）。** 低于 70 → 回 Phase 2 迭代，**迭代上限 2 轮**；两轮后仍不达标，在诚实边界中标注薄弱维度，交付当前最优版本。

**原型型与虚构型**：立场一致性维度改为「与档案/设定的一致性」（见评分卡的「档案一致性」维度说明）。

#### 记录到评测历史

`FIDELITY.md` 每次重铸都被覆盖，**分数历史会丢**。每次评分后追加一条：

```bash
python3 scripts/eval_record.py record --file ~/.claude/skills/<slug>-persona/EVALS.jsonl --json '{...}'
```

记录必须含 `version` / `artifact_sha256` / `models`（答题与评分两个模型）/ `scorers` / **内嵌题目集**。

> **题目内嵌在记录里，不做全局题库。** 人格会变，全局固定题库会与之脱节。
> 尤其：超范围题的正确答案是「拒答 + 标注推断」；当档案补上该领域后正确答案变成「真答」，
> 而评分规则仍把拒答记为正确 → **越完整的人格分越低**（假退步）。
> 所以该领域被覆盖后要把该题标 `retired`。

---

### Phase 4.5: 对抗精炼

读 `references/adversarial-refine.md`。评分之后加一道对抗环：

| | 攻击者 A | 攻击者 B |
|---|---------|---------|
| summon | **保真度攻击者**（编造特质、`gaps` 未映射、立场漂移） | **可用性攻击者**（角色扮演规则漏洞、形容词式表达DNA、空转的 Protocol 维度、缺失 Fallback） |

**四条护栏**：

1. **必须交补丁** — before/after 文本 + 指名的 rubric 维度 + 被攻击行号；缺任一项**直接丢弃**
2. **攻击者拿 ground truth** — 喂源档案 `DISTILLATE.md`。没有它，攻击者只能挑风格
3. **只许删改，不许新增断言** — 尤其**不许为了让「人格更完整」而添加立场**：那会直接撞上「档案一致性 = 0 → 判 D」的红线
4. **不做「必须提升否则回滚」的评分闸门** — 单次评分噪声 ±5–10 分，闸门会一半概率放过假修复、一半概率回滚真修复

**产出是候选补丁清单**（`REFINE.md`），交用户裁决。质量信号由**第二独立评分 agent** 提供。

**迭代上限 2 轮**，对齐「宁可交付诚实的 60 分」。

---

### Phase 5: 召唤

读 `references/summon-protocol.md`。四种阵型：

| 阵型 | 人数 | 做什么 | 关键机制 |
|------|------|--------|---------|
| **单召** | 1 | 人格直接接管对话 | **不 spawn**——主 agent 直接演 |
| **双人对谈** | 2 | 各自独立作答，呈现分歧 | 并行 spawn，互不可见 |
| **圆桌** | 2-5 | 多轮互质 | 第 1 轮互不可见，第 2 轮注入他人发言 |
| **遣将** | 2-N | 分兵处理不同子问题后拼合 | 每人拿**不同**子问题 |

#### 主持人的三条硬约束

1. **不扮演人格**——主持人是编排者，不是演员
2. **防串味**——每个 persona agent 的 prompt 里只给一个人格文件路径；圆桌第 2 轮的他人发言由主持人**显式粘贴**，不共享上下文
3. **不调和分歧**——汇总时分歧本身就是产出，不要揉成一个温吞共识

#### 有 panel 时：第 2 轮 seeding

若存在对应的 `panels/<slug>.panel.md`：

1. **第 1 轮照常盲测** —— 各成员拿原问题，**互不可见**，**不读 panel**
2. 主持人读 panel 的 `agenda`（**成员看不到**）
3. **第 2 轮**把 agenda 里的议题作为「你们可能分歧的地方」注入 prompt，逼交锋打在真断层线上
4. 汇总时标注哪些分歧是**真断层线**（价值观分歧），哪些只是**事实分歧**

> **panel 只给主持人看。** 一旦把它注入人格，第 1 轮的盲测就废了，
> 而且人格会为了配合编排去断言档案里没有的立场 → 判 D。

**降级**：环境不支持并行 spawn → 串行轮流扮演，但**必须明确告知用户**「同一 agent 连续扮演多角，独立性下降」。

---

### Phase 6: 交付

**🔴 CHECKPOINT · 展示保真度评分卡与人格卡给用户确认后才算完成。**

交付：
- `~/.claude/skills/<slug>-persona/SKILL.md` — 人格运行体
- `FIDELITY.md` — 保真度评分卡
- 源档案链接 — `distilled/<slug>/`

**登记名册**：跑 `python3 scripts/roster.py` 更新名册，或按 `references/roster-format.md` 手动登记。

**告知用户怎么用**：
- 单召 → 「用 XX 的视角看看…」
- 对谈 → 「让 XX 和 YY 都说说」
- 圆桌 → 「开个圆桌，让 XX、YY、ZZ 互相质疑」
- 遣将 → 「分头看看这个方案」

#### 6.1 记录使用反馈（写给自己的目录）

使用中暴露的失败，写进**人格自己的目录**（不是档案目录）：

```bash
python3 scripts/feedback_log.py --persona-dir ~/.claude/skills/<slug>-persona \
    --failure in_scope_gap --question "..." --evidence "档案 §决策启发式 第3条" \
    --formation roundtable --archive-sha <sha>
```

**失败类型分两类**：

| 类别 | 类型 | 计入缺陷汇总？ |
|------|------|--------------|
| 人格缺陷 | `style_drift` / `in_scope_gap` / `wrong_stance` | ✅ |
| **忠实沉默** | `faithful_silence`（档案已标注该缺口 / 主体主动不公开 → **拒答是正确行为**） | ❌ **不计入** |

> **`faithful_silence` 不是缺陷。** 拒答在 gaps 映射、结构性沉默、伦理红线场景下都是对的。
> 把它当缺陷会逼 distill 去「补上」这个缺口，从而**为刻意沉默的人编造立场**——
> 那正是保真度评分卡判 0 分的行为。

**两条硬性纪律**：
- **必须带 `--evidence` 证据指针**（档案条目/行号/出处）。无证据 = 意见，脚本**拒绝写入**（铁律3）
- **单召不记录**——单召不 spawn，主 agent 就是人格，中途记录等于跳出角色（反模式 #10）。单召的失败留到事后复盘用 `--formation review` 记

**归属约束**：脚本会**拒绝**把反馈写进含 `DISTILLATE.md` 的目录。档案归 distill 管，人格归 summon 管。

**闭环**：反馈文件本身**不会**触发漂移检测（它不改档案哈希）。真实链路是
`用户跑 distill 补充蒸馏 → distill 只读汇总 → 更新 gaps → 哈希变 → roster 报 stale → 重铸`。
触发点是 **distill 的动作**，需要一次人工介入——不是全自动。

---

## 失败模式与降级路径

| 触发条件 | 一线修复 | 仍失败兜底 |
|---------|---------|-----------|
| 无法并行 spawn subagent | 串行轮流扮演 | **明确告知用户独立性下降**，建议减少同场人格数 |
| 上下文窗口不足 | 单召优先；圆桌降到 2 人 2 轮 | 分会话跑，人格文件本身就是断点 |
| 源档案缺失（真人型） | 引导先跑 蒸馏.skill | 降级为原型型，并明确声明「非真人还原」 |
| 档案维度不足 | 回 蒸馏.skill 补维度 | 降级为原型型 + 加厚诚实边界 |
| **只铸出认知层**（有框架、没有「我」） | 回 Phase 2 补身份卡 + 表达DNA | 素材确无表达特征 → 降级为原型型，并声明「这是合成框架，不是一个人」 |
| **只铸出人格层**（语气像、看问题没区别） | 回 Phase 2 补心智模型 + 启发式 | 素材确无骨架 → 回 蒸馏.skill 补，别硬铸 |
| 人格跑偏（变通用 AI 腔） | 回读「表达DNA」段重新对齐 | 重跑，prompt 里显式粘贴表达DNA |
| 人格串味（多人越聊越像） | 检查是否共享了上下文 | 重跑第 1 轮，强化隔离约束 |
| 保真度 <70 | 回 Phase 2 迭代 | 迭代 2 轮后标注薄弱维度交付 |
| 用户要求铸在世的非公众人物 | 要求提供素材 + 征得同意 | **拒绝**，说明隐私边界 |
| 用户要求冒充他人 | 明确拒绝 | 说明伦理红线，不提供变通方案 |

---

## 反模式黑名单（绝不做的事）

| # | 反模式 | 为什么 / 替代做法 |
|---|--------|------------------|
| 1 | 把真人当传声筒替本人表态 | 未表态领域必须标「框架推断」；结构性沉默要忠实呈现 |
| 2 | 无档案硬铸真人型人格 | 那是在凭训练语料编造。先跑 蒸馏.skill，或降级为原型型 |
| 3 | 诚实边界不映射档案 gaps | 诚实边界是地基。`gaps` 一条不许少 |
| 4 | 自评保真度 | 答题与评分必须分离，自评接近随机 |
| 5 | 人格串味 | 每个 agent 只读自己的人格文件；圆桌第 1 轮互不可见 |
| 6 | 汇总时把分歧调和成共识 | 分歧就是产出。主持人不要和稀泥 |
| 7 | 虚构型复现原作品长段原文 | 版权。只做设定内推演 |
| 8 | 原型型不声明自己是合成的 | 必须声明「不对应任何具体个人」 |
| 9 | 医疗/法律/投资人格不给免责 | 伦理红线，硬性要求 |
| 10 | 召唤时丢掉角色扮演规则与诚实边界 | 长对话极易漂移回通用助手腔 |
| 11 | `description` 塞长尾关键词 | 超 1024 字会报错，且抬高误触发率。真正命中的是人名和专属概念 |
| 12 | 把 persona 装进项目目录却当全局用 | 明确装到 `~/.claude/skills/`（用户级）或项目级，别混 |
| 13 | 用户已有人格还重复铸造 | 先扫描 `*-persona/`，已有就直接召唤（零成本） |
| 14 | **把 panel 注入人格的 prompt** | 人格会知道自己「应该反对谁」→ 破坏**独立首次**（第 1 轮盲测作废）+ **替人格表态**（档案一致性 = 0 → 判 D）。panel 只给主持人看 |
| 15 | 把「忠实沉默」记成缺陷 | 拒答在 gaps 映射、结构性沉默、伦理红线场景下**是正确行为**。记成缺陷会逼 distill 为刻意沉默的人编造立场 → 判 0 分 |
| 16 | 无证据指针就写反馈 | 无出处的反馈是**意见**不是缺陷。`feedback_log.py` 会拒绝写入（铁律3） |
| 17 | 用单次评分当精炼闸门 | 单次 LLM 评分噪声 ±5–10 分。质量信号应来自第二独立评分 agent，不是攻击者 |
| 18 | 缺口/领域被覆盖后不退役题目 | 超范围题的正确答案会从「拒答」变成「真答」，不标 `retired` 就会得出「越完整分越低」的假退步 |
| 19 | 声称「写了反馈就会自动触发重铸」 | 反馈文件不改档案哈希，**不会**触发漂移检测。需要用户跑一次 distill 补充蒸馏，链路才动 |
| 20 | **交付只有一层的产物**（只有认知框架，或只有说话语气） | 双核缺一不可：只有框架召不出「谁」，只有语气给不出新视角。两层都在才算人格运行体 |

---

## 最后

拘神遣将不是造几个会说话的壳子，也不是造几套没有「人」的框架。

一个好的人格，让你**借一双别人的眼睛**看自己的问题，并且用**那个人的方式**把话说出来；
好的圆桌，让**几个视角互相打磨**，磨掉你自己看不见的盲点。

## 版本自检（静默）

本节给 AI agent：每次使用本 skill 前，先做一个不联网的判断——读本 skill 目录（SKILL.md 所在目录）下的 `.last-update-check` 文件（内容为一行日期 YYYY-MM-DD）。文件存在且距今不足 30 天，跳过本节直接干活，不向用户提任何更新相关的话。

到期或文件不存在时，且仅在此时：

1. 本目录不是 git 克隆（无 `.git` 或无 origin）→ 写入今天日期后跳过本节
2. 对比版本：`git -C <本目录> rev-parse HEAD` 与 `git -C <本目录> ls-remote origin HEAD`
3. 无论结果如何，把今天日期写入 `.last-update-check`
4. 两者一致 → 什么都不说；确认落后 → 先完成用户当前任务，结束后附一句「本 skill 有新版本，可用 `git -C <本目录> pull --ff-only` 更新」。是否更新由用户决定，不要主动执行更新
